# Ads Agent compatibility entry

The consolidated multi-product Ads Agent now lives at:

<https://github.com/SolveaCX/gtm-swarm/tree/main/apps/ads-agent>

This legacy directory is retained so existing links and historical commits remain understandable. Its `agent.yaml`, metrics, content-bank directories, and product-specific campaign text are not the runtime source of truth. Agent identity and state come from Multica/11Agents; shared runtime code and reviewed product packages come from `apps/ads-agent/`.
